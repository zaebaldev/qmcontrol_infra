services:
  gateway:
    image: devopsfaith/krakend:latest
    network_mode: host
    volumes:
      - ./krakend:/etc/krakend
      - ./krakend/jwks.json:/etc/krakend/jwks.json
    ports:
      - 8080:8080